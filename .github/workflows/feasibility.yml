name: Prompt 2 feasibility

on:
  pull_request:
    paths:
      - ".github/workflows/feasibility.yml"
      - "tools/feasibility/**"
      - "docs/design/**"
  push:
    branches:
      - agent/prompt-2-feasibility

permissions:
  contents: read

jobs:
  deterministic-feasibility:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate canonical data
        run: python tools/feasibility/validate_data.py

      - name: Run unit tests
        run: python -m unittest discover -s tools/feasibility/tests -v

      - name: Run mandatory deterministic scenarios
        run: python tools/feasibility/run_scenarios.py --mandatory --quiet
